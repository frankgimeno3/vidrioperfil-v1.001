import React, { FC } from 'react';
import Layout from '../layout';

interface AlProps {
  
}

const Al: FC<AlProps> = ({ }) => {
  return (
    <Layout>
      <div>
        Al
      </div>
    </Layout>  );
};

export default Al;