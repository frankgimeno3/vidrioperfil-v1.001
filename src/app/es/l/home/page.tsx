import Layout from '@/app/layout';
import React, { FC } from 'react';

interface HomeProps {
  
}

const Home: FC<HomeProps> = ({ }) => {
  return (
    <Layout>
      <div>
        Es
      </div>
    </Layout>  );
};

export default Home;