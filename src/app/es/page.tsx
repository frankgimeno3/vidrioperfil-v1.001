import React, { FC } from 'react';
import Layout from '../layout';

interface EsProps {

}

const Es: FC<EsProps> = ({ }) => {
  return (
    <Layout>
      <div>
        Es
      </div>
    </Layout>
  );
};

export default Es;