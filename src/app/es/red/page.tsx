import Layout from '@/app/layout';
import React, { FC } from 'react';

interface RedProps {
  
}

const Red: FC<RedProps> = ({ }) => {
  return (
    <Layout>
      <div>
        Red
      </div>
    </Layout>  );
};

export default Red;