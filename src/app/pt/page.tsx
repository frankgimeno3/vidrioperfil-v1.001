import React, { FC } from 'react';
import Layout from '../layout';

interface PtProps {
  
}

const Pt: FC<PtProps> = ({ }) => {
  return (
    <Layout>
      <div>
        Pt
      </div>
    </Layout>  );
};

export default Pt;