import React, { FC } from 'react';

interface MessagesIconProps {
  
}

const MessagesIcon: FC<MessagesIconProps> = ({ }) => {
  return (
    <div>MessagesIcon</div>
  );
};

export default MessagesIcon;